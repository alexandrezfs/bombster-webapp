<section>

    <div class="container">
        <div class="row">

            <h1>{{question.question_title}}</h1>

            <div class="col-lg-12">

                <form action="/question/delete" method="POST">
                    <input type="hidden" name="question_id" value="{{question._id}}">
                    <input type="submit" class="btn" value="Delete this question">
                </form>

            </div>
        </div>
    </div>

</section>